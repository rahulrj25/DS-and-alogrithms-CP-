long long power(int x, unsigned int y)
{
    long long res = 1;      // Initialize result
 
    x = x % p; 
    while (y > 0)
    {
       
        if (y & 1)
            res = (res*x) % p;
        y = y>>1; // y = y/2
        x = (x*x) % p;  
    }
    return res;
}